package org.hibernate;

import idv.people.model.*;
import idv.people.util.PeopleSessionFactory;

public class HibernateTest {

	public static void main(String[] args) {
		
//		Session session = PeopleSessionFactory.getSessionFactory().openSession();
//		Transaction transaction = session.beginTransaction();
//		
//		People people =(People) session.load(People.class, 10011);
//		session.delete(people);
//		transaction.commit();
//		
//		session.close();

//		Session session = PeopleSessionFactory.getSessionFactory().openSession();
//		Transaction transaction = session.beginTransaction();
//
//		People people = (People) session.load(People.class, 10010);	// 執行這行會產生 SELECT 的語句
//		people.setName("王聰明");	// 執行這行會產生 UPDATE 的語句
//		transaction.commit();
//
//		session.close();
		
	}

}
